# MySQL connection (used by backend)
MYSQL_USER=root
MYSQL_PASSWORD=secret
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DB=srisaikitchen

# Frontend: point to backend API
VITE_API_BASE_URL=http://localhost:8000

# Optional CORS/dev helpers:
# Set to 1 to allow all origins during local development (not for production)
# DEV_ALLOW_ALL_CORS=1

# Or add a comma-separated list of extra frontend origins to allow, e.g.:
# VITE_FRONTEND_ORIGINS=http://localhost:8081,http://localhost:3000
